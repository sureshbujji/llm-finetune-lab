# CPU-only torch: the +cpu local version exists only on the PyTorch index,
# so pip resolves torch from there and everything else from PyPI.
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.4.1+cpu
transformers==4.44.2
peft==0.12.0
numpy==1.26.4
pytest==8.3.4
